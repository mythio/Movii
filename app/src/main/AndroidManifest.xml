<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mythio.movii">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,InnerclassSeparator">
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.discover.activity.DiscoverActivity" />
        <activity
            android:name=".activity.movie_details.MovieDetailsActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.tv_show_details.TvShowDetailsActivity" />
        <activity
            android:name=".activity.search_movie.SearchMovieActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activity.search_tv_show.SearchTvShowActivity"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>